import React, { useState } from 'react';
import { 
  StyleSheet, 
  View, 
  Text, 
  ScrollView, 
  Image, 
  TouchableOpacity, 
  SafeAreaView,
  Platform,
  StatusBar
} from 'react-native';
import { SafeAreaProvider, useSafeAreaInsets } from 'react-native-safe-area-context';
import { 
  Map, 
  ListChecks, 
  Search, 
  Wallet, 
  Edit2, 
  User,
  Users,
  ChevronDown,
  Plane,
  Clock,
  Utensils,
  MapPin
} from 'lucide-react-native';
import { BlurView } from 'expo-blur';

// 模拟数据
const tripData = {
  name: '马来西亚之旅',
  cover: 'https://images.unsplash.com/photo-1567157577867-05ccb1388e66?auto=format&fit=crop&q=80&w=400',
  itinerary: [
    { id: 1, title: '抵达与市内深度游', date: '4月25日', location: '亚庇' },
    { id: 2, title: '大王花 & 红树林', date: '4月26日', location: '亚庇' },
  ]
};

const AppContent = () => {
  const insets = useSafeAreaInsets();
  const [activeTab, setActiveTab] = useState('itinerary');

  return (
    <View style={styles.container}>
      <StatusBar barStyle="dark-content" />
      
      {/* Header - 处理灵动岛 */}
      <View style={[styles.header, { paddingTop: insets.top + 10 }]}>
        <View>
          <View style={styles.brandRow}>
            <View style={styles.pulseDot} />
            <Text style={styles.brandText}>TRAVELBUDDY</Text>
          </View>
          <Text style={styles.headerTitle}>智能协作旅行助手</Text>
        </View>
        <View style={styles.headerActions}>
          <TouchableOpacity style={styles.editButton}>
            <Edit2 size={14} color="#64748b" strokeWidth={3} />
            <Text style={styles.editButtonText}>编辑</Text>
          </TouchableOpacity>
          <View style={styles.avatar}>
            <User size={20} color="#94a3b8" />
          </View>
        </View>
      </View>

      <ScrollView 
        style={styles.scrollView}
        contentContainerStyle={{ paddingBottom: insets.bottom + 100 }}
      >
        {/* Hero Card */}
        <View style={styles.heroContainer}>
          <Image source={{ uri: tripData.cover }} style={styles.heroImage} />
          <View style={styles.heroOverlay}>
            <View style={styles.tagRow}>
              <View style={styles.statusTag}>
                <Text style={styles.statusTagText}>进行中</Text>
              </View>
              <View style={styles.collaborators}>
                <Users size={12} color="rgba(255,255,255,0.6)" />
                <Text style={styles.collaboratorsText}>4 位协作者</Text>
              </View>
            </View>
            <Text style={styles.heroTitle}>{tripData.name}</Text>
            <Text style={styles.heroSub}>4月25日 — 5月5日 · 11 天深度探索</Text>
          </View>
        </View>

        {/* 行程列表示例 */}
        {tripData.itinerary.map((day) => (
          <View key={day.id} style={styles.dayCard}>
            <View style={styles.dayHeader}>
              <View style={styles.dayBadge}>
                <Text style={styles.dayBadgeText}>DAY</Text>
                <Text style={styles.dayBadgeNum}>{day.id}</Text>
              </View>
              <View style={styles.dayInfo}>
                <Text style={styles.dayLocation}>{day.location}</Text>
                <Text style={styles.dayTitle}>{day.title}</Text>
              </View>
              <ChevronDown size={20} color="#cbd5e1" />
            </View>
          </View>
        ))}
      </ScrollView>

      {/* Bottom Nav - 处理底部安全区 */}
      <View style={[styles.bottomNavContainer, { paddingBottom: insets.bottom + 10 }]}>
        <BlurView intensity={80} tint="light" style={styles.blurNav}>
          <TouchableOpacity onPress={() => setActiveTab('itinerary')} style={styles.navItem}>
            <Map size={20} color={activeTab === 'itinerary' ? '#2563eb' : '#94a3b8'} />
            <Text style={[styles.navText, activeTab === 'itinerary' && styles.navTextActive]}>行程</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => setActiveTab('checklist')} style={styles.navItem}>
            <ListChecks size={20} color={activeTab === 'checklist' ? '#2563eb' : '#94a3b8'} />
            <Text style={[styles.navText, activeTab === 'checklist' && styles.navTextActive]}>清单</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => setActiveTab('research')} style={styles.navItem}>
            <Search size={20} color={activeTab === 'research' ? '#2563eb' : '#94a3b8'} />
            <Text style={[styles.navText, activeTab === 'research' && styles.navTextActive]}>调研</Text>
          </TouchableOpacity>
          <TouchableOpacity onPress={() => setActiveTab('expenses')} style={styles.navItem}>
            <Wallet size={20} color={activeTab === 'expenses' ? '#2563eb' : '#94a3b8'} />
            <Text style={[styles.navText, activeTab === 'expenses' && styles.navTextActive]}>分账</Text>
          </TouchableOpacity>
        </BlurView>
      </View>
    </View>
  );
};

export default function ExpoApp() {
  return (
    <SafeAreaProvider>
      <AppContent />
    </SafeAreaProvider>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#f8fafc',
  },
  header: {
    backgroundColor: 'rgba(255,255,255,0.8)',
    flexDirection: 'row',
    justifyContent: 'space-between',
    alignItems: 'center',
    paddingHorizontal: 24,
    paddingBottom: 16,
    borderBottomWidth: 1,
    borderBottomColor: '#e2e8f0',
  },
  brandRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 2,
  },
  pulseDot: {
    width: 8,
    height: 8,
    borderRadius: 4,
    backgroundColor: '#3b82f6',
    marginRight: 8,
  },
  brandText: {
    fontSize: 10,
    fontWeight: '900',
    color: '#94a3b8',
    letterSpacing: 2,
  },
  headerTitle: {
    fontSize: 18,
    fontWeight: '900',
    color: '#0f172a',
  },
  headerActions: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  editButton: {
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#f1f5f9',
    paddingHorizontal: 12,
    paddingVertical: 8,
    borderRadius: 12,
    marginRight: 12,
  },
  editButtonText: {
    fontSize: 12,
    fontWeight: '900',
    color: '#64748b',
    marginLeft: 6,
  },
  avatar: {
    width: 40,
    height: 40,
    borderRadius: 16,
    backgroundColor: '#f1f5f9',
    justifyContent: 'center',
    alignItems: 'center',
    borderWidth: 1,
    borderColor: '#e2e8f0',
  },
  scrollView: {
    flex: 1,
  },
  heroContainer: {
    margin: 16,
    height: 220,
    borderRadius: 40,
    overflow: 'hidden',
    elevation: 10,
    shadowColor: '#3b82f6',
    shadowOffset: { width: 0, height: 10 },
    shadowOpacity: 0.1,
    shadowRadius: 20,
  },
  heroImage: {
    width: '100%',
    height: '100%',
  },
  heroOverlay: {
    ...StyleSheet.absoluteFillObject,
    backgroundColor: 'rgba(15, 23, 42, 0.4)',
    padding: 24,
    justifyContent: 'flex-end',
  },
  tagRow: {
    flexDirection: 'row',
    alignItems: 'center',
    marginBottom: 8,
  },
  statusTag: {
    backgroundColor: '#3b82f6',
    paddingHorizontal: 10,
    paddingVertical: 4,
    borderRadius: 20,
    marginRight: 8,
  },
  statusTagText: {
    color: 'white',
    fontSize: 10,
    fontWeight: '900',
  },
  collaborators: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  collaboratorsText: {
    color: 'rgba(255,255,255,0.6)',
    fontSize: 10,
    fontWeight: '700',
    marginLeft: 4,
  },
  heroTitle: {
    fontSize: 28,
    fontWeight: '900',
    color: 'white',
    marginBottom: 4,
  },
  heroSub: {
    fontSize: 12,
    color: 'rgba(255,255,255,0.7)',
    fontWeight: '600',
  },
  dayCard: {
    backgroundColor: 'white',
    marginHorizontal: 16,
    marginBottom: 12,
    borderRadius: 24,
    padding: 16,
    borderWidth: 1,
    borderColor: '#f1f5f9',
  },
  dayHeader: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  dayBadge: {
    width: 48,
    height: 48,
    backgroundColor: '#eff6ff',
    borderRadius: 16,
    justifyContent: 'center',
    alignItems: 'center',
    marginRight: 16,
  },
  dayBadgeText: {
    fontSize: 8,
    fontWeight: '900',
    color: '#3b82f6',
  },
  dayBadgeNum: {
    fontSize: 18,
    fontWeight: '900',
    color: '#3b82f6',
  },
  dayInfo: {
    flex: 1,
  },
  dayLocation: {
    fontSize: 10,
    fontWeight: '900',
    color: '#3b82f6',
    letterSpacing: 1,
    marginBottom: 2,
  },
  dayTitle: {
    fontSize: 14,
    fontWeight: '700',
    color: '#1e293b',
  },
  bottomNavContainer: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    paddingHorizontal: 24,
    paddingTop: 12,
  },
  blurNav: {
    flexDirection: 'row',
    justifyContent: 'space-around',
    alignItems: 'center',
    backgroundColor: 'rgba(255,255,255,0.7)',
    borderRadius: 32,
    paddingVertical: 12,
    borderWidth: 1,
    borderColor: 'rgba(255,255,255,0.4)',
    overflow: 'hidden',
  },
  navItem: {
    alignItems: 'center',
    justifyContent: 'center',
    paddingHorizontal: 16,
  },
  navText: {
    fontSize: 9,
    fontWeight: '900',
    color: '#94a3b8',
    marginTop: 4,
    letterSpacing: 1,
  },
  navTextActive: {
    color: '#2563eb',
  }
});
